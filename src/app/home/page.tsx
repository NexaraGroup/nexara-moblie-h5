'use client';

import Button from '@/components/button';

const PageHome = () => {
	return (
		<Button type="text" onClick={() => {}} block>
			Forgot Password?
		</Button>
	);
};
export default PageHome;
